import 'package:flutter/material.dart';

class W11Widget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'W11',
        style: TextStyle(fontSize: 24, color: Colors.black),
      ),
    );
  }
}
