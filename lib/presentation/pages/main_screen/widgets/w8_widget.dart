import 'package:flutter/material.dart';

class W8Widget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'W8',
        style: TextStyle(fontSize: 24, color: Colors.black),
      ),
    );
  }
}
