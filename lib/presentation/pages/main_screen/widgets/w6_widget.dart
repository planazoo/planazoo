import 'package:flutter/material.dart';

class W6Widget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'W6',
        style: TextStyle(fontSize: 24, color: Colors.black),
      ),
    );
  }
}
