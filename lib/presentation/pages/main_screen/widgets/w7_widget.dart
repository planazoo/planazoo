import 'package:flutter/material.dart';

class W7Widget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'W7',
        style: TextStyle(fontSize: 24, color: Colors.black),
      ),
    );
  }
}
