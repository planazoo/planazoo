import 'package:flutter/material.dart';

class W10Widget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'W10',
        style: TextStyle(fontSize: 24, color: Colors.black),
      ),
    );
  }
}
